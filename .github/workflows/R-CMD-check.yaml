# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

name: R-CMD-check

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    environment: ${{ github.event_name == 'pull_request' && 'ci-approval' || 'ci-auto' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Environment gate passed"

  R-CMD-check:
    needs: gate
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }}) ${{ matrix.config.custom }}

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: macos-latest,   r: 'release'}

          - {os: windows-2022, r: 'release'}
          
          # use 4.1 to check with rtools40's older compiler
          - {os: windows-2022, r: '4.1'}
          
          # Use older ubuntu to maximise backward compatibility
          - {os: ubuntu-22.04,   r: 'devel', http-user-agent: 'release'}
          - {os: ubuntu-22.04,   r: 'release', custom: 'no-later2test'}
          - {os: ubuntu-22.04,   r: 'oldrel-1'}
          - {os: ubuntu-22.04,   r: 'oldrel-2'}
          - {os: ubuntu-22.04,   r: 'oldrel-3'}
          - {os: ubuntu-22.04,   r: 'oldrel-4'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v5

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: false

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          pak-version: devel
          needs: check

      - name: Install later2test
        if: matrix.config.custom != 'no-later2test'
        run: |
          options(warn = 2)
          pak::pkg_install("pachadotdev/cpp4r")
          install.packages("./", repos = NULL, type = "source")
          pak::local_install_dev_deps("later2test", dependencies = TRUE)
          install.packages("./later2test", repos = NULL, INSTALL_opts = "--install-tests", type = "source")
        shell: Rscript {0}

      - uses: r-lib/actions/check-r-package@v2
        with:
          build_args: 'c("--no-manual","--compact-vignettes=gs+qpdf")'

      - name: Run later2test tests
        if: matrix.config.custom != 'no-later2test'
        run: |
          tinytest::build_install_test("later2test")
        shell: Rscript {0}
